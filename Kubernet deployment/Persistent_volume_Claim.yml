apiVersion: v1
kind: PersistentVolumeclaim
metadata: 
  name: mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce 
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeName: mysql-pv
